name: ccl
source_type: retailer
base_url: https://www.cclonline.com
search_url: https://www.cclonline.com/search/{query}
selectors:
  card: article, .product, .product-item, .search-result
  title: a[href] h3, a[href] .title, h3, .product-title
  price: .price, .product-price, [data-testid="price"]
  url: a[href]
  stock: .stock, .availability, .product-stock, [data-testid="stock"]
  image: img
  id: a[href]
# The configured endpoint returns hardened Cloudflare protection that exceeds
# Byparr bypass capabilities (direct HTTP 403 domain-wide, Byparr timeouts >120s).
# Keep the connector explicit and truthful until a supported access lane is supplied.
disabled: true
disabled_reason: "hardened Cloudflare protection (HTTP 403 domain-wide, Byparr anti-bot route times out >120s). See internal diagnosis notes for details."
use_flare_solverr: true
category: gpu
